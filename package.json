{
  "name": "blockingjs",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon ./index.js ",
    "ui": "browser-sync start --config bs-config.js",
    "prod": "npm run prod"
    // use pm2 to give it commands to run in prod/continuously
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.27.9",
    "nodemon": "^2.0.16"
  },
  "dependencies": {
    "express": "^4.18.1",
    "pug": "^3.0.2",
    "pm2": "5.2.0"
  }
}
